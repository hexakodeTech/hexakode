import { Job } from "@/types/careers";

/**
 * Mock job database - currently empty to trigger the "Open Roles" empty state.
 * Populate this array when active openings are available.
 */
export const jobs: Job[] = [];
