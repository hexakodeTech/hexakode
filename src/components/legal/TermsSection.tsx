// Re-exports PrivacySection — the component is fully generic (PolicySection props).
// Using an alias keeps the Terms architecture explicit without duplicating code.
export { default } from "./PrivacySection";
